
export class AIDifficulty extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( AIDifficulty.values[ i ] !== undefined ) return AIDifficulty.values[ i ];
		return AIDifficulty.values[ i ] = new AIDifficulty( i );

	}

}

export class AllianceType extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( AllianceType.values[ i ] !== undefined ) return AllianceType.values[ i ];
		return AllianceType.values[ i ] = new AllianceType( i );

	}

}

export class AnimType extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( AnimType.values[ i ] !== undefined ) return AnimType.values[ i ];
		return AnimType.values[ i ] = new AnimType( i );

	}

}

export class AttackType extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( AttackType.values[ i ] !== undefined ) return AttackType.values[ i ];
		return AttackType.values[ i ] = new AttackType( i );

	}

}

export class BlendMode extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( BlendMode.values[ i ] !== undefined ) return BlendMode.values[ i ];
		return BlendMode.values[ i ] = new BlendMode( i );

	}

}

export class CameraField extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( CameraField.values[ i ] !== undefined ) return CameraField.values[ i ];
		return CameraField.values[ i ] = new CameraField( i );

	}

}

export class DamageType extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( DamageType.values[ i ] !== undefined ) return DamageType.values[ i ];
		return DamageType.values[ i ] = new DamageType( i );

	}

}

export class DialogEvent extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( DialogEvent.values[ i ] !== undefined ) return DialogEvent.values[ i ];
		return DialogEvent.values[ i ] = new DialogEvent( i );

	}

}

export class EffectType extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( EffectType.values[ i ] !== undefined ) return EffectType.values[ i ];
		return EffectType.values[ i ] = new EffectType( i );

	}

}

export class FGameState extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( FGameState.values[ i ] !== undefined ) return FGameState.values[ i ];
		return FGameState.values[ i ] = new FGameState( i );

	}

}

export class FogState extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( FogState.values[ i ] !== undefined ) return FogState.values[ i ];
		return FogState.values[ i ] = new FogState( i );

	}

}

export class GameDifficulty extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( GameDifficulty.values[ i ] !== undefined ) return GameDifficulty.values[ i ];
		return GameDifficulty.values[ i ] = new GameDifficulty( i );

	}

}

export class GameEvent extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( GameEvent.values[ i ] !== undefined ) return GameEvent.values[ i ];
		return GameEvent.values[ i ] = new GameEvent( i );

	}

}

export class GameSpeed extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( GameSpeed.values[ i ] !== undefined ) return GameSpeed.values[ i ];
		return GameSpeed.values[ i ] = new GameSpeed( i );

	}

}

export class GameType extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( GameType.values[ i ] !== undefined ) return GameType.values[ i ];
		return GameType.values[ i ] = new GameType( i );

	}

}

export class IGameState extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( IGameState.values[ i ] !== undefined ) return IGameState.values[ i ];
		return IGameState.values[ i ] = new IGameState( i );

	}

}

export class ItemType extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( ItemType.values[ i ] !== undefined ) return ItemType.values[ i ];
		return ItemType.values[ i ] = new ItemType( i );

	}

}

export class LimitOp extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( LimitOp.values[ i ] !== undefined ) return LimitOp.values[ i ];
		return LimitOp.values[ i ] = new LimitOp( i );

	}

}

export class MapControl extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( MapControl.values[ i ] !== undefined ) return MapControl.values[ i ];
		return MapControl.values[ i ] = new MapControl( i );

	}

}

export class MapDensity extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( MapDensity.values[ i ] !== undefined ) return MapDensity.values[ i ];
		return MapDensity.values[ i ] = new MapDensity( i );

	}

}

export class MapFlag extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( MapFlag.values[ i ] !== undefined ) return MapFlag.values[ i ];
		return MapFlag.values[ i ] = new MapFlag( i );

	}

}

export class MapSetting extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( MapSetting.values[ i ] !== undefined ) return MapSetting.values[ i ];
		return MapSetting.values[ i ] = new MapSetting( i );

	}

}

export class MapVisibility extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( MapVisibility.values[ i ] !== undefined ) return MapVisibility.values[ i ];
		return MapVisibility.values[ i ] = new MapVisibility( i );

	}

}

export class MouseButtonType extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( MouseButtonType.values[ i ] !== undefined ) return MouseButtonType.values[ i ];
		return MouseButtonType.values[ i ] = new MouseButtonType( i );

	}

}

export class PathingType extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( PathingType.values[ i ] !== undefined ) return PathingType.values[ i ];
		return PathingType.values[ i ] = new PathingType( i );

	}

}

export class Placement extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( Placement.values[ i ] !== undefined ) return Placement.values[ i ];
		return Placement.values[ i ] = new Placement( i );

	}

}

export class PlayerColor extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( PlayerColor.values[ i ] !== undefined ) return PlayerColor.values[ i ];
		return PlayerColor.values[ i ] = new PlayerColor( i );

	}

}

export class PlayerEvent extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( PlayerEvent.values[ i ] !== undefined ) return PlayerEvent.values[ i ];
		return PlayerEvent.values[ i ] = new PlayerEvent( i );

	}

}

export class PlayerGameResult extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( PlayerGameResult.values[ i ] !== undefined ) return PlayerGameResult.values[ i ];
		return PlayerGameResult.values[ i ] = new PlayerGameResult( i );

	}

}

export class PlayerScore extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( PlayerScore.values[ i ] !== undefined ) return PlayerScore.values[ i ];
		return PlayerScore.values[ i ] = new PlayerScore( i );

	}

}

export class PlayerSlotState extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( PlayerSlotState.values[ i ] !== undefined ) return PlayerSlotState.values[ i ];
		return PlayerSlotState.values[ i ] = new PlayerSlotState( i );

	}

}

export class PlayerState extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( PlayerState.values[ i ] !== undefined ) return PlayerState.values[ i ];
		return PlayerState.values[ i ] = new PlayerState( i );

	}

}

export class PlayerUnitEvent extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( PlayerUnitEvent.values[ i ] !== undefined ) return PlayerUnitEvent.values[ i ];
		return PlayerUnitEvent.values[ i ] = new PlayerUnitEvent( i );

	}

}

export class Race extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( Race.values[ i ] !== undefined ) return Race.values[ i ];
		return Race.values[ i ] = new Race( i );

	}

}

export class RacePref extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( RacePref.values[ i ] !== undefined ) return RacePref.values[ i ];
		return RacePref.values[ i ] = new RacePref( i );

	}

}

export class RarityControl extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( RarityControl.values[ i ] !== undefined ) return RarityControl.values[ i ];
		return RarityControl.values[ i ] = new RarityControl( i );

	}

}

export class SoundType extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( SoundType.values[ i ] !== undefined ) return SoundType.values[ i ];
		return SoundType.values[ i ] = new SoundType( i );

	}

}

export class StartLocPrio extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( StartLocPrio.values[ i ] !== undefined ) return StartLocPrio.values[ i ];
		return StartLocPrio.values[ i ] = new StartLocPrio( i );

	}

}

export class SubAnimType extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( SubAnimType.values[ i ] !== undefined ) return SubAnimType.values[ i ];
		return SubAnimType.values[ i ] = new SubAnimType( i );

	}

}

export class TexMapFlags extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( TexMapFlags.values[ i ] !== undefined ) return TexMapFlags.values[ i ];
		return TexMapFlags.values[ i ] = new TexMapFlags( i );

	}

}

export class UnitEvent extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( UnitEvent.values[ i ] !== undefined ) return UnitEvent.values[ i ];
		return UnitEvent.values[ i ] = new UnitEvent( i );

	}

}

export class UnitState extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( UnitState.values[ i ] !== undefined ) return UnitState.values[ i ];
		return UnitState.values[ i ] = new UnitState( i );

	}

}

export class UnitType extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( UnitType.values[ i ] !== undefined ) return UnitType.values[ i ];
		return UnitType.values[ i ] = new UnitType( i );

	}

}

export class Version extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( Version.values[ i ] !== undefined ) return Version.values[ i ];
		return Version.values[ i ] = new Version( i );

	}

}

export class VolumeGroup extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( VolumeGroup.values[ i ] !== undefined ) return VolumeGroup.values[ i ];
		return VolumeGroup.values[ i ] = new VolumeGroup( i );

	}

}

export class WeaponType extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( WeaponType.values[ i ] !== undefined ) return WeaponType.values[ i ];
		return WeaponType.values[ i ] = new WeaponType( i );

	}

}

export class WidgetEvent extends Number {

	static values = [];

	static fromInteger( i ) {

		if ( WidgetEvent.values[ i ] !== undefined ) return WidgetEvent.values[ i ];
		return WidgetEvent.values[ i ] = new WidgetEvent( i );

	}

}

export default {
	ConvertAIDifficulty: i => AIDifficulty.fromInteger( i ),
	ConvertAllianceType: i => AllianceType.fromInteger( i ),
	ConvertAnimType: i => AnimType.fromInteger( i ),
	ConvertAttackType: i => AttackType.fromInteger( i ),
	ConvertBlendMode: i => BlendMode.fromInteger( i ),
	ConvertCameraField: i => CameraField.fromInteger( i ),
	ConvertDamageType: i => DamageType.fromInteger( i ),
	ConvertDialogEvent: i => DialogEvent.fromInteger( i ),
	ConvertEffectType: i => EffectType.fromInteger( i ),
	ConvertFGameState: i => FGameState.fromInteger( i ),
	ConvertFogState: i => FogState.fromInteger( i ),
	ConvertGameDifficulty: i => GameDifficulty.fromInteger( i ),
	ConvertGameEvent: i => GameEvent.fromInteger( i ),
	ConvertGameSpeed: i => GameSpeed.fromInteger( i ),
	ConvertGameType: i => GameType.fromInteger( i ),
	ConvertIGameState: i => IGameState.fromInteger( i ),
	ConvertItemType: i => ItemType.fromInteger( i ),
	ConvertLimitOp: i => LimitOp.fromInteger( i ),
	ConvertMapControl: i => MapControl.fromInteger( i ),
	ConvertMapDensity: i => MapDensity.fromInteger( i ),
	ConvertMapFlag: i => MapFlag.fromInteger( i ),
	ConvertMapSetting: i => MapSetting.fromInteger( i ),
	ConvertMapVisibility: i => MapVisibility.fromInteger( i ),
	ConvertMouseButtonType: i => MouseButtonType.fromInteger( i ),
	ConvertPathingType: i => PathingType.fromInteger( i ),
	ConvertPlacement: i => Placement.fromInteger( i ),
	ConvertPlayerColor: i => PlayerColor.fromInteger( i ),
	ConvertPlayerEvent: i => PlayerEvent.fromInteger( i ),
	ConvertPlayerGameResult: i => PlayerGameResult.fromInteger( i ),
	ConvertPlayerScore: i => PlayerScore.fromInteger( i ),
	ConvertPlayerSlotState: i => PlayerSlotState.fromInteger( i ),
	ConvertPlayerState: i => PlayerState.fromInteger( i ),
	ConvertPlayerUnitEvent: i => PlayerUnitEvent.fromInteger( i ),
	ConvertRace: i => Race.fromInteger( i ),
	ConvertRacePref: i => RacePref.fromInteger( i ),
	ConvertRarityControl: i => RarityControl.fromInteger( i ),
	ConvertSoundType: i => SoundType.fromInteger( i ),
	ConvertStartLocPrio: i => StartLocPrio.fromInteger( i ),
	ConvertSubAnimType: i => SubAnimType.fromInteger( i ),
	ConvertTexMapFlags: i => TexMapFlags.fromInteger( i ),
	ConvertUnitEvent: i => UnitEvent.fromInteger( i ),
	ConvertUnitState: i => UnitState.fromInteger( i ),
	ConvertUnitType: i => UnitType.fromInteger( i ),
	ConvertVersion: i => Version.fromInteger( i ),
	ConvertVolumeGroup: i => VolumeGroup.fromInteger( i ),
	ConvertWeaponType: i => WeaponType.fromInteger( i ),
	ConvertWidgetEvent: i => WidgetEvent.fromInteger( i )
};
