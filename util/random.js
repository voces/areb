
// export const linear = ( spread = 1 ) => ( Math.random() - 0.5 ) * 2 * spread;
// export const binomial = ( spread = 1 ) => ( Math.random() - 0.5 ) * ( Math.random() - 0.5 ) * 4 * spread;

// // (base + flat) * percent
// export const random = ( base, flat = 0, percent = 0 ) =>
// 	( base +
// 		( Math.random() - 0.5 ) * 2 * flat ) *
// 		( 1 + ( Math.random() - 0.5 ) * 2 * percent );

// export const randomizer = ( flat, percent ) =>
// 	v => random( v, flat, percent );

